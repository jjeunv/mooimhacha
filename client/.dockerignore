# 빌드 컨텍스트에서 제외 (이미지 슬림화 + 비밀/캐시 유출 방지)
node_modules
dist
dist-electron
release

# 환경 변수 / 비밀 — VITE_* 값은 build-arg로 주입
.env
.env.*

# 로그
logs
*.log

# 테스트 산출물
test-results
playwright-report
test/screenshots
test/*.png

# Electron 데스크탑 전용 자산 (웹 빌드에 불필요)
electron
electron-builder.json
resources

# 메타/문서
.git
.gitignore
Dockerfile
.dockerignore
README.md
